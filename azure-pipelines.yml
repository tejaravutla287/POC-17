trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  # Replace these with your exact Azure Service Connection details
  azureServiceConnection: 'MyAzureServiceConnection' 
  resourceGroupName: 'rg-terraform-backend' # RG where your backend storage lives
  storageAccountName: 'stpoc17backend'       # Unique storage account for tfstate
  containerName: 'tfstate'
  tfstateKey: 'poc17.tfstate'

stages:
- stage: Build
  displayName: 'Build and Package Code'
  jobs:
  - job: Pack
    steps:
    # Package HTML/JS source code
    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: 'src'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/app.zip'
        replaceExistingArchive: true

    # Publish App Artifact
    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)/app.zip'
        ArtifactName: 'drop-app'
        publishLocation: 'Container'

    # Publish Terraform files as an artifact
    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: 'terraform'
        ArtifactName: 'drop-terraform'
        publishLocation: 'Container'

- stage: DeployInfrastructure
  displayName: 'Provision Infrastructure'
  dependsOn: Build
  jobs:
  - job: Terraform
    steps:
    - task: DownloadBuildArtifacts@0
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop-terraform'
        downloadPath: '$(System.ArtifactsDirectory)'

    # Initialize Terraform using Native Task
    - task: TerraformTaskV4@4
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.ArtifactsDirectory)/drop-terraform'
        backendServiceArm: '$(azureServiceConnection)'
        backendAzureRmResourceGroupName: '$(resourceGroupName)'
        backendAzureRmStorageAccountName: '$(storageAccountName)'
        backendAzureRmContainerName: '$(containerName)'
        backendAzureRmKey: '$(tfstateKey)'

    # Apply Infrastructure using Native Task
    - task: TerraformTaskV4@4
      inputs:
        provider: 'azurerm'
        command: 'apply'
        workingDirectory: '$(System.ArtifactsDirectory)/drop-terraform'
        environmentServiceNameAzureRM: '$(azureServiceConnection)'

- stage: DeployApp
  displayName: 'Deploy Application Code'
  dependsOn: DeployInfrastructure
  jobs:
  - job: Deploy
    steps:
    - task: DownloadBuildArtifacts@0
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop-app'
        downloadPath: '$(System.ArtifactsDirectory)'

    # Deploy ZIP package to the newly created Free App Service
    - task: AzureRmWebAppDeployment@4
      inputs:
        ConnectionType: 'AzureRM'
        azureSubscription: '$(azureServiceConnection)'
        appType: 'webApp'
        WebAppName: 'webapp-poc17-unique-name' # Must match your variable string exactly
        Package: '$(System.ArtifactsDirectory)/drop-app/app.zip'
